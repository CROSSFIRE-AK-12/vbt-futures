[pytest]
testpaths = tests
addopts =
    --cov=src/vbt_futures
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=100
    --cov-branch
    -ra
markers =
    slow: cross-tool comparison tests (vs backtrader)
env =
    NUMBA_DISABLE_JIT=1
